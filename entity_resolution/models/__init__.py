"""The three methods (``exact_v1``, ``rules_v1``, ``learned_v1``), their version records and the
shared tiering step. Method implementations arrive in Phase 4; ``tiering`` holds the decision
constants from docs/BRIEF.md section 2.8 and ``registry`` reads and writes the version records."""
